interface:
  display_name: 'Frontend Testing'
  short_description: 'Plan and test frontend behavior at the right layer'
  default_prompt: 'Use $testing to create the test plan, implement the approved test layer, run it, and report evidence for this frontend behavior.'
