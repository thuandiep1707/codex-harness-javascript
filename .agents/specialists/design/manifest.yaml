name: design
role: design-provider-specialist
description: Produce traceable external design artifacts from one bounded Jira Subtask handoff.
accepts:
  - issue-handoff
produces:
  - design-artifact
  - agent-report
context:
  allowed:
    - runtime-issue-handoff
  forbidden:
    - <working-project>/.docs/**
    - <working-project>/source-mutation
    - local-workflow-state
skills:
  - orchestrate-frontend-design
rules:
  - .agents/specialists/design/rules/context-boundary.md
capabilities:
  required:
    - design-provider
  optional: []
