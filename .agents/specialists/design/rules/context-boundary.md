# Design context boundary

- Never open, search, summarize, or quote `.docs/`.
- Use the issue handoff as the complete product brief.
- Return a context-insufficient blocker instead of retrieving extra project documents.
- Do not treat provider-generated code as approved production source.
- Preserve stable provider, file, project, node, revision, and preview identifiers.

